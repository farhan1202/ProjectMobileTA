<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dev.projectta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".home.ResultActivity"></activity>
        <activity android:name=".home.CandidateActivity" />
        <activity android:name=".home.VoteActivity" />
        <activity
            android:name=".home.AboutUsActivity"
            android:parentActivityName=".home.MainActivity" />
        <activity android:name=".intro.RegisterActivity" />
        <activity android:name=".intro.LoginActivity" />
        <activity android:name=".home.MainActivity" />
        <activity android:name=".intro.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>